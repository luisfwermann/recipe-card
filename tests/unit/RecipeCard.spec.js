// import { mount } from "@vue/test-utils";
// import RecipeCard from "../../src/components/RecipeCard.vue";

describe("RecipeCard.vue", () => {
  test("energy is right when user config is for showing calories", () => {
    expect(1).toEqual(1);
  });
});
